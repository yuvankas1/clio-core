# IOWarp Core - ROCm Variant
# ROCm-enabled debug build using CMakePresets.json "rocm-debug" preset
#
# Usage: IOWARP_PRESET=rocm-debug conda build installers/conda/ -c conda-forge

preset:
  - rocm-debug

cuda_enabled:
  - "OFF"
rocm_enabled:
  - "ON"
mpi_enabled:
  - "OFF"
mpi_impl:
  - nompi
