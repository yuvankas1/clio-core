# Pipeline: PyFLEXTRKR workload, Apptainer + sbatch on Ares, 4 nodes,
# with IOWarp's FUSE adapter providing the CTE-backed mount under
# ${HOME}/clio_cte_pyflextrkr_4n.
#
# Multi-host via the mcs_imerg demo. The 4-node SLURM allocation is
# fanned across via PsshExecInfo; per-rank NetCDF feature/mask writes
# land in the FUSE mountpoint and route through clio_cte → clio_runtime.

name: pyflextrkr_iowarp_libfuse_4n

install_manager: container
tmp_bind_root: "/mnt/nvme/${USER}"
container_engine: apptainer
container_image: pyflextrkr_apptainer_test

container_binds:
  # pyflextrkr_io_only.sh override removed — the SIF's real /opt/run_demo.sh
  # runs the genuine PyFLEXTRKR demo (mcs_imerg). Re-add the io_only bind
  # to switch back to the I/O proxy.
  - ${HOME}/jarvis-runs:${HOME}/jarvis-runs
  - ${HOME}/clio_cte_pyflextrkr_4n:/mnt/clio_cte
  - ${HOME}/clio_cte_pyflextrkr_4n:${HOME}/clio_cte_pyflextrkr_4n

ssh_cmd: "env -u LD_LIBRARY_PATH ssh"

mpi_cmd: 'mpiexec --mca btl_tcp_if_include enp47s0np0 --mca oob_tcp_if_include eno1 --prtemca prte_if_include eno1 --prtemca prte_tmpdir_base /home/llogan/iowarp_prte_tmp'

env:
  TMPDIR: "/home/llogan/iowarp_prte_tmp"
  CLIO_MEMFD_DIR: "/home/llogan/iowarp_chimaera_tmp_${HOSTNAME}"
  PYF_TRACK_FRAMES: "8"
  PYF_TRACK_MB: "512"
  PYF_MCS_FRAMES: "8"
  PYF_MCS_MB: "1024"
  PYF_STATS_FILES: "8"
  PYF_STATS_MB: "1024"

scheduler:
  name: slurm
  job_name: pyflextrkr_iowarp_libfuse_4n
  nodes: 4
  ntasks_per_node: 24
  partition: compute
  nodelist: "ares-comp-[14-17]"
  time: "00:10:00"
  output: slurm-%j.out
  error: slurm-%j.err
  suffix: "-40g"
  pre_cmds:
    - 'source /etc/profile.d/lmod.sh 2>/dev/null || source /etc/profile.d/modules.sh'
    - 'module load gcc/11.4.0/openmpi/5.0.5-x2kvx5l'
    - 'mkdir -p /home/llogan/iowarp_prte_tmp'
    - 'fusermount3 -u /home/llogan/clio_cte_pyflextrkr_4n 2>/dev/null || umount -l /home/llogan/clio_cte_pyflextrkr_4n 2>/dev/null || true'
    - 'mkdir -p /home/llogan/clio_cte_pyflextrkr_4n'

pkgs:
  - pkg_type: jarvis_clio_core.clio_runtime
    pkg_name: runtime
    deploy_mode: default
    num_threads: 4
    client_data_segment_size: "2G"
    port: 9413
    ipc_mode: shm
    log_level: info
    sleep: 4
    swim_enabled: false

  - pkg_type: jarvis_clio_core.clio_cte
    pkg_name: cte_core
    deploy_mode: default
    devices:
      - ["ram::cte_ram_tier1", "25GB", 1.0]
    dpe_type: max_bw
    neighborhood: 1
    default_target_timeout_ms: 30000
    poll_period_ms: 5000
    iowarp_compress: none
    pool_query: "local"

  - pkg_type: jarvis_clio_core.clio_cte_libfuse
    pkg_name: cte_fuse
    deploy_mode: default
    mountpoint: ${HOME}/clio_cte_pyflextrkr_4n
    sleep: 2
    extra_fuse_args: "-f"

  - pkg_type: builtin.pyflextrkr
    pkg_name: pyflextrkr
    nprocs: 1
    ppn: 1
    demo: mcs_imerg
    replicates: 8
    # parallel_reps>1 switches to PsshExecInfo + per-host wait -n batching.
    # 8 reps / 4 nodes = 2 per host; parallel_reps=2 keeps 2 Dask local
    # clusters in flight per host (each consuming omp_threads cores).
    parallel_reps: 2
    omp_threads: 4
    out: ${HOME}/clio_cte_pyflextrkr_4n/pyflextrkr
